MYSQL_HOST=127.0.0.1
MYSQL_PORT=3306
MYSQL_USER=root
MYSQL_PASSWORD=
AIRFLOW_BASE_URL=http://localhost:8080

# Optional contact address sent to OpenAlex/Crossref by the `enrich` stage to
# use their polite pools (faster, more reliable). Left empty, both APIs still
# answer from the shared anonymous pool.
ENRICHMENT_MAILTO=
